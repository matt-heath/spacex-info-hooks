import React from 'react';

class ResourceList extends React.Component {
    render() {
        return (
            <>
                {this.props.resource}
            </>
        );
    }
}

export default ResourceList;
